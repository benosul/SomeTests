name: Run Some Tests
on:
  workflow_dispatch:

jobs:
  build-and-commit:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Test Docker Pull wo login
        run: |
          docker pull ghcr.io/benosul/sometests:main
          docker run sometests:main 
